"""Concrete strategy implementations, one subpackage per strategy (e.g. strategy_1).

Only strategy_1 is built in this first pass; strategy_registry.py is stubbed to
prove registration works cleanly, without building strategies 2/3/4 yet."""
